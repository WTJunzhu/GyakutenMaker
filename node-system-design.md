# 节点系统设计 · Node System Design

> 本文档专门讨论 GyakutenMaker 编辑器的**节点系统**——它是整个制作器的核心。
> 记录作者（用户）的原始诉求，并在其上做系统性扩展。属于阶段2~4 的设计蓝图，
> 与 `project-plan.md` 的路线互补。数据结构演进需与 `renpy/common/00aa_runtime.rpy`
> 运行时解释器保持兼容（编辑器只产出数据，运行时读数据）。

---

## 一、为什么节点系统是核心

逆转裁判的剧本不是线性的对话流，而是一张**因果网络**：

- 第 3 天法庭上要举证的那把刀，是第 1 天搜证时捡到的；
- 证人第 4 句证言的矛盾，依赖玩家是否在此前拿到了验尸报告；
- 一个「目击者其实是凶手」的反转，需要前面埋下十几处看似无关的细节。

作者面对的真实困难是：

1. **篇幅长**——一个章节动辄上百个节点，纯靠连线顺序根本找不到目标。
2. **环环相扣**——改一个早期设定，可能牵动后面一连串引用（证据、flag、证言）。
3. **需要瞻前顾后**——写到后面时，经常要回头确认「这个证据到底拿到没有」「这个 flag 之前设过吗」。

所以节点系统的目标**不止是"能连线"，而是"能驾驭复杂度"**：组织、导航、筛选、搜索、引用追踪、一致性校验。做好了，作者省时间、少出逻辑漏洞；做不好，编辑器还不如记事本。

---

## 二、作者的原始诉求（用户想法记录）

> 以下为用户 2026-08-28 提出的原始想法，原样保留，后续所有设计以此为根。

1. **并列分支**：节点除了"前后两条连线"，还应支持**并列的分支节点**（一个节点引出多个后继）。
2. **排序不能只靠连线顺序**：节点多了以后，一堆节点堆在一起，很难找到特定位置，需要额外的组织/排序方式。
3. **多维筛选**：
   - 按**事件类型**筛选（对话 / 证言 / 搜证 …）；
   - 按**发生时间**筛选（剧情时序）；
   - **关键词模糊搜索**具体节点。
4. **典型场景**：写到案件某部分需要某个证据，但不确定之前有没有让玩家拿到——希望能**直接搜索/查看**这个证据在哪里被获得、被使用。
5. **总目标**：节点系统好用 = 作者省时间 + 情节更合理（少逻辑漏洞）。

---

## 三、设计原则

| 原则 | 含义 |
|------|------|
| **数据与视图分离** | `case.json` 只存事实（节点、连线、引用）；组织/筛选/排序是编辑器的"视图层"能力，尽量不污染运行时数据。 |
| **多视图看同一份数据** | 同一批节点，可用「画布图」「大纲列表」「时间线」「表格」等多种方式查看，各有所长。 |
| **引用是一等公民** | 证据、flag、角色、地点的"定义处"和"使用处"必须被系统显式建模，才能做追踪、校验、跳转。 |
| **渐进不破坏** | 新增字段一律可选，旧 `case.json` 不改也能打开；`_editor` / `_meta` 命名空间存编辑器专用数据，运行时忽略。 |
| **降低出错优先于花哨** | 优先做能"防止逻辑漏洞"的功能（校验、引用追踪），其次才是体验美化。 |

---

## 四、数据模型演进

### 4.1 节点元数据（`_meta`）

给每个节点加一个可选的 `_meta`，承载"组织与检索"所需信息（运行时忽略）：

```jsonc
"node_id": {
  "type": "get_evidence",
  "evidence_ids": ["thinker"],
  "next": "...",
  "_editor": { "x": 800, "y": 400 },
  "_meta": {
    "title": "捡到小雕像",          // 人类可读标题（列表/搜索/画布显示，比 id 友好）
    "note": "关键证据，第5句证言举证用",  // 作者备注（只给作者看，不入游戏）
    "tags": ["关键证据", "第一天"],   // 自由标签，可用于筛选
    "group": "day1_investigation",   // 所属分组/环节（见 4.3）
    "story_time": 101,               // 剧情时序权重（见 六.1 时间线）
    "status": "done",                // draft | wip | done —— 创作进度
    "color": "#2ecc40"               // 覆盖默认类型色（可选，用于视觉标记重点）
  }
}
```

> 现有 `title` 字段（testimony 用）与 `_meta.title` 不冲突：前者是游戏内证言标题，后者是编辑器里的节点名。列表优先显示 `_meta.title`，缺省回退到 `title` 或节点 id。

### 4.2 多出口连线（端口模型）

现状：连线只有 `next`（单出口）+ choice 的 `options[].next`。这不足以表达逆转裁判的分支。

**方案：把"出口"抽象成命名端口（port）**。一个节点可以有多个具名出口：

| 节点类型 | 出口（port） | 语义 |
|----------|-------------|------|
| dialogue / get_evidence / set_flag | `next` | 唯一后继 |
| choice | `option[0]`, `option[1]`… | 每个选项一个出口 |
| testimony | `next`(默认结束)、`present[句号]`(举证成功跳转)、`gameover`(血量归零) | 多结局 |
| condition（新增，见七） | `true` / `false` | 条件分支 |
| investigation | `next`(退出搜证) | 单出口（内部热点不算流程分支） |

画布上每个 port 是节点下沿一个独立的连接点，连线记录 `(source, sourcePort, target)`。这样并列分支、条件分支都能自然表达，也为"分支合流"（多个节点汇聚到同一后继）铺路。

> 数据落地：分支类节点的出口本就写在各自字段里（`options[].next` / `present_handlers` 的 `on_correct.next`）。端口模型主要是**编辑器画布层**的呈现与交互，`deriveEdges()` 已在做类似的事，扩展它即可，不必大改 `case.json`。

### 4.3 组织层级：章节 / 环节 / 分组

一个案件不该是一锅节点。引入**分组**层级来对抗"节点太多找不到"：

```
案件 Case
 └─ 章节 Episode（第1章：初次的逆转）
     └─ 环节 Scene（法庭·上午 / 侦探·公寓 / 法庭·下午）
         └─ 节点 Node（对话/证言/搜证…）
```

数据上用轻量方式实现（不强制嵌套，避免破坏 flat 的 `nodes` map）：

```jsonc
"_groups": {
  "day1_court_am": { "title": "第1天·法庭上午", "color": "#f39c12", "order": 1, "collapsed": false },
  "day1_investigation": { "title": "第1天·搜证", "color": "#16a085", "order": 2 }
},
```

节点通过 `_meta.group` 归属分组。画布上分组表现为**可折叠的容器框**（React Flow 支持 group/parent node）；大纲列表里表现为可折叠的树。这样作者可以"折叠掉已完成的环节，只看当前在写的部分"。

---

## 五、组织与导航

### 5.1 大纲面板（Outline / 节点列表）★

在画布左侧加一个**大纲面板**——这是解决"找不到节点"最直接的手段：

- 按分组树状展示所有节点，每项显示：图标（类型色）+ 标题 + 状态徽标；
- 点击项 → 画布聚焦并选中该节点（camera 平滑移动过去）；
- 支持拖拽调整分组归属、排序；
- 顶部带搜索框与筛选器（见六）；
- 折叠/展开分组。

大纲面板 + 画布双向联动（选中互相同步），是长章节导航的骨架。

### 5.2 多视图

同一份节点数据，提供多个视角切换：

1. **画布视图（Graph）**——现有，看拓扑结构与分支。
2. **大纲视图（Outline）**——树状列表，快速定位与批量管理。
3. **时间线视图（Timeline）**——按 `_meta.story_time` 横向排列，看剧情时序（见六.1）。
4. **表格视图（Table）**——每行一个节点，列为 id/类型/标题/分组/状态/引用数，适合批量审阅与排序，可按任意列排序。

### 5.3 画布增强

- **迷你地图**（已有）+ **书签/锚点**：作者给重要节点打书签，一键跳转。
- **面包屑**：显示当前聚焦的 章节 > 环节，点击可上钻。
- **自动布局**：一键按拓扑（分层 / 时序）重排节点位置，缓解"手动摆放乱"。
- **连线高亮**：选中一个节点，高亮其所有入边/出边链路（追溯"从哪来、到哪去"）。

---

## 六、筛选与搜索（作者诉求核心）

### 6.1 筛选维度

在大纲/画布顶部提供筛选器，命中的节点高亮或仅显示，其余淡化：

| 维度 | 说明 |
|------|------|
| **类型** | 对话 / 证言 / 搜证 / … 多选（用户明确要求）。 |
| **剧情时间** | 按 `_meta.story_time` 或所属"天/时段"筛选（用户明确要求）。 |
| **分组/环节** | 只看某章某环节。 |
| **标签** | 按 `_meta.tags` 筛（如"关键证据""伏笔""待完善"）。 |
| **创作状态** | 只看 `draft` / `wip`，快速找到"还没写完"的坑。 |
| **引用某资源** | 只看**引用了某证据/flag/角色**的节点（见 6.3，这是杀手锏）。 |
| **孤儿/断链** | 只看没有入边（除入口）或出口指向不存在节点的问题节点。 |

**剧情时间**说明：逆转裁判天然有"第几天 / 上午下午 / 回忆闪回"的时间结构。建议 `_meta` 支持结构化时间：`day`(第几天) + `phase`(上午/下午/回忆) + `story_time`(细粒度排序权重)。时间线视图与时间筛选都基于它。

### 6.2 全文搜索

一个全局搜索框，模糊匹配（用户明确要求）：

- 搜**节点标题 / id / 备注 / 标签**；
- 搜**台词内容**（对话行、证言句、追问/举证台词）——"我记得有句台词提到了钥匙，在哪？"；
- 搜**引用的资源 id**（证据/角色/地点/flag）；
- 结果列表按匹配位置分组，点击直达节点并高亮命中片段。

### 6.3 引用追踪（伏笔回收面板）★★★ 核心中的核心

这是直接回应用户那个典型场景——**"这个证据之前拿到没有？"**——的功能，也是本设计最有价值的部分。

系统为每种"资源"建立**定义处 ↔ 使用处**的双向索引：

| 资源 | 定义/产生处 | 使用/消费处 |
|------|------------|------------|
| **证据 Evidence** | `get_evidence` 节点、搜证热点的 `get_evidence` | testimony 举证的 `correct_evidence`、talk 的 present、condition 判断"是否持有" |
| **Flag** | `set_flag` 节点 | condition 节点的判断、分支条件 |
| **角色 Character** | 角色定义表 | 所有出现该角色台词的节点 |
| **地点 Location** | 地点定义表 | investigation 的 `location_id`、move 节点 |

基于此提供一个**「引用检查器」面板**：选中任意资源（比如证据 `thinker`），面板列出：

```
证据「小雕像 (thinker)」
  ● 获得于：
      - [搜证] 雕像底座 (day1_investigation)   ← 点击跳转
  ● 被使用于：
      - [举证] 证言"事件目击"第5句 (day1_court)  ← 点击跳转
      - [对话探索] 向矢张出示 (day2_detention)
  ⚠ 警告：使用处(day1_court)在剧情时间上早于获得处？→ 逻辑漏洞提示
```

这一个面板同时解决了：
- "证据拿到没有"（看有没有获得处）；
- "拿到的证据用没用上"（有获得无使用 = 可能多余）；
- "用到的证据是否已获得"（有使用无获得 = **逻辑漏洞**，玩家不可能通关）；
- **时序合理性**（使用早于获得 = bug）。

> 这是把"作者的记忆负担"转化为"系统的自动索引"，是节点系统真正的降本增效点。

---

## 七、更多节点类型（扩展提案）

现有 8 种：dialogue / get_evidence / set_flag / penalty / testimony / investigation / talk / choice。
建议逐步补充（优先级：★必要 / ☆增强 / ○远期）：

| 新类型 | 优先级 | 说明 | 出口 |
|--------|--------|------|------|
| **condition 条件分支** | ★ | 根据 flag / 是否持有某证据，走不同后继。逆转裁判大量"是否触发过某剧情"的判断依赖它。 | `true` / `false` |
| **merge / label 汇流点** | ★ | 多个分支汇聚到同一后继，避免重复连线到同一节点造成的混乱。也可作跳转锚点。 | `next` |
| **comment 注释节点** | ★ | 纯作者备注，不入游戏。用于在画布上写"这里待补充""设定说明"。 | 无 |
| **ending 结局** | ★ | 显式标记流程终点（有罪/无罪/BadEnd），配合校验"是否所有分支都有结局"。 | 无 |
| **psyche_lock 心理锁** | ☆ | 逆转裁判2+ 招牌玩法：出示证据逐个击破心锁。数据类似 testimony 但机制不同。 | `unlocked` / `fail` |
| **objection 演出** | ☆ | 异议！/ 等等！全屏演出、震屏、音效，作为独立可复用的演出节点。 | `next` |
| **move 移动** | ☆ | 侦探部分在多个地点间移动（地图选点），每个地点关联一个 investigation。 | 多出口（每地点） |
| **cross_exam_branch 证言分支** | ○ | 逆转检事式：证言过程中插入询问再回到证言。 | 复杂 |
| **subflow 子流程** | ○ | 把一组节点封装成可复用/可折叠的子图（如"标准法庭开场"模板）。 | `next` |
| **variable 变量运算** | ○ | 数值变量增减（好感度、线索计数），支撑更复杂玩法。 | `next` |

> `set_flag` + `condition` 组合已能覆盖大量"分支"需求，应优先落地这两个的可视化。

---

## 八、一致性校验与健康度

节点系统应主动"体检"，把逻辑漏洞在编辑期就暴露（阶段3 校验的深化）：

**结构类**
- 断链：`next` / `option.next` / `on_correct.next` 指向不存在的节点。
- 孤儿：除入口外，没有任何入边的节点（玩家永远走不到）。
- 无出口：非 ending 节点却没有任何后继（流程意外中断）。
- 不可达：从 entry 做图遍历，标出到不了的节点。
- 环检测：非预期的死循环。

**引用类**（基于六.3 的索引）
- 使用了未获得的证据（**致命**，卡关）。
- 判断了从未 set 过的 flag。
- 引用了不存在的角色/地点 id。
- 时序倒挂：使用处剧情时间早于定义处。

**创作类**
- 举证节点未设置任何 `correct_evidence`（玩家无法通过）。
- 存在 `draft`/`wip` 状态节点的统计（进度看板）。

呈现：画布上问题节点标红角标；顶部"问题面板"列出全部，点击直达。健康度可给个总分/进度条，让作者对章节完成度有整体感。

---

## 九、让节点系统更好用的其他想法

1. **节点标题优先显示**：画布卡片和列表默认显示 `_meta.title` 而非 id，作者一眼看懂"这是干嘛的"。
2. **快捷创建 + 自动连线**：选中节点按 Tab/Enter 直接在其后新建并连好线，键盘流快速铺剧情。
3. **节点复制/模板**：把常用结构（如"证人登场→证言→反诘"）存为模板，一键展开一组节点。
4. **撤销/重做**：长时间创作必备，基于 store 快照或 immer patch。
5. **批量操作**：框选多个节点，批量改分组、加标签、改颜色、移动。
6. **连线标注**：分支连线上显示条件/选项文字（choice 已有），condition 显示"持有雕像时"。
7. **拆分/合并节点**：一段超长对话可拆成多个；或把碎节点合并。
8. **差异对比 / 版本快照**：给章节存快照，改崩了能回滚；对比两版流程差异。
9. **协作注释**：给节点加"待办/评论"，多人协作或自我提醒。
10. **导出流程图**：把画布导出为图片/PDF，便于离线审阅整章结构。
11. **模拟走查（Dry-run）**：不启动 Ren'Py，在编辑器里"假装玩"一遍——按选择/举证走分支，实时显示当前持有的证据/flag，快速验证某条路径是否走得通。这是引用追踪的动态版，极适合验证"这条分支玩家能不能通"。
12. **热点可视化摆放**：investigation 的热点坐标，未来在背景图上直接拖拽标注，而非填数字。

---

## 十、分期实施建议

> 遵循"先降低出错、再提升体验"，且每步都可独立交付、不破坏现有数据。

### 与 project-plan.md 开发阶段的对齐

节点系统**不是一个独立的开发阶段**，而是拆散后贴合现有路线图自然生长：

| 本文批次 | 对应项目阶段 | 说明 |
|----------|-------------|------|
| 第一批·第1项（`_meta` + 标题显示） | **阶段2 收尾** | 成本极低、是导航地基，随阶段2 一起做掉 |
| 第一批·第2~4项（大纲/搜索/筛选） | 阶段3 | 路线图「工程内搜索」`[进阶]` |
| 第二批（引用追踪 + 校验 + 基础新节点） | **阶段3 核心** | 与阶段3「缺失证物引用/断链/未设举证」校验完全吻合，跟素材库一起长出来 |
| 第三批（分组/时间线/表格视图） | 阶段3 后期 → 阶段4 | 组织类，需长章节实践后再做才不跑偏 |
| 第四批（撤销重做/走查/高级节点） | 阶段4 | 对应路线图「高级演出与特殊玩法」 |

> 结论：现在只需完成第一批第1项作为阶段2 收尾；节点系统的"魂"（引用追踪、校验）是阶段3 的事。

**第一批（马上能显著提效，改动小）**
1. ✅ `_meta` 元数据（title / note / status）+ 画布优先显示 title、id 作副标题、状态徽标。（2026-08-28 完成，阶段2 收尾）
2. **大纲面板**（树状列表 + 点击定位 + 折叠）。
3. **全文搜索**（标题/台词/id/引用）。
4. **类型筛选 + 状态筛选**。

**第二批（核心价值，中等改动）**
5. **引用追踪索引 + 引用检查器面板**（证据/flag 的定义↔使用双向）。
6. **一致性校验 + 问题面板**（断链/孤儿/使用未获得证据）。
7. `condition` + `merge` + `comment` + `ending` 四种基础新节点及可视化。

**第三批（组织与视图，较大改动）**
8. 分组容器（画布可折叠 group）+ 分组管理。
9. 时间线视图 + 剧情时间字段与筛选。
10. 表格视图、自动布局、连线链路高亮。

**第四批（进阶体验）**
11. 撤销/重做、批量操作、节点模板、模拟走查（Dry-run）。
12. 版本快照/差异、导出流程图、心理锁/异议等高级节点。

---

## 附：数据结构草案（增量，向后兼容）

```jsonc
{
  "version": "1.1",              // 引入 _meta/_groups 后 bump
  "id": "gs1_ep1",
  "title": "第1话 最初的逆转",
  "entry": "intro",

  "_groups": {                    // 编辑器专用：分组/环节定义
    "day1_court": { "title": "第1天·法庭", "color": "#f39c12", "order": 1 }
  },

  "nodes": {
    "get_thinker": {
      "type": "get_evidence",
      "evidence_ids": ["thinker"],
      "next": "court_start",
      "_editor": { "x": 800, "y": 400 },
      "_meta": {
        "title": "捡到小雕像",
        "note": "第5句证言举证用",
        "tags": ["关键证据", "伏笔"],
        "group": "day1_investigation",
        "day": 1, "phase": "am", "story_time": 101,
        "status": "done"
      }
    },

    // 新增：条件分支
    "check_thinker": {
      "type": "condition",
      "cond": { "kind": "has_evidence", "id": "thinker" },  // 或 { kind:"flag", key:"...", equals:true }
      "true": "reveal_scene",
      "false": "hint_scene",
      "_meta": { "title": "是否已持有雕像" }
    },

    // 新增：汇流/锚点
    "after_court": { "type": "merge", "next": "day2_start", "_meta": { "title": "法庭结束汇合" } },

    // 新增：注释（不入游戏）
    "todo_note": { "type": "comment", "_meta": { "title": "TODO: 补充矢张的反应台词" } },

    // 新增：结局
    "not_guilty": { "type": "ending", "outcome": "not_guilty", "_meta": { "title": "无罪结局" } }
  }
}
```

**运行时兼容性**：`_groups` / `_meta` / `_editor` 一律以 `_` 前缀，`00aa_runtime.rpy` 的 `dispatch()` 只认 `type` 并读各自已知字段，未知字段自动忽略；`condition`/`merge`/`comment`/`ending` 需要在解释器里各加一个 `exec_*` 处理器（comment 直接跳 next 或无操作，merge 等价透传，condition 求值选 port，ending 结束）。改动集中在解释器新增分支，不影响既有节点。

---

## 十一、一句话总结

> 把"作者脑子里那张关系网"变成"系统可查询、可校验、可导航的结构"——
> 这就是节点系统要做的全部事情。连线只是它最表层的样子。
