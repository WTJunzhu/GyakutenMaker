# GyakutenMaker — 逆转裁判同人游戏可视化制作器

## 一、项目愿景

做一个**可视化、无需编码**的桌面制作器，让「想做逆转裁判风格剧情、但不会写代码做游戏」的玩家，能够像使用视频剪辑软件一样，通过拖拽节点、填写表单、导入素材的方式，制作出带有搜证、法庭、举证、异议演出等逆转裁判原作味道的同人游戏。

**目标用户（按优先级）：**

1. **主要用户** — 有剧情创意、但不会编程的逆转裁判玩家。他们能想到「谁在什么时候说什么、玩家要出示哪个证物」，但不想学脚本语言。
2. **次要用户** — 会写代码的玩家。编辑器对他们同样可用；如果他们想在导出结果上做更深度的二次开发（改 Ren'Py 脚本、加自定义玩法），那是他们自己的自由，编辑器不阻止也不特别照顾。

**一句话定位：** 逆转裁判版的「RPG Maker / Ren'Py 可视化前端」。

---

## 二、为什么基于 Ren'Py

> 说明：本项目**不自研游戏引擎**。Ren'Py 是成熟的开源视觉小说引擎，负责跨平台运行、存档/读档/回滚、音画渲染、打包分发。我们只在其上叠加「逆转裁判特有机制」和「可视化编辑器」。

- **运行时成熟**：存档、回滚、跨平台打包（Windows/Mac/Linux/Web/Android）Ren'Py 都已解决，无需重造。
- **可扩展**：通过 `renpy/common/` 放置自定义 Statement、Screen、Displayable，不必 fork 引擎核心。
- **社区与文档完善**：遇到问题有大量参考。
- **我们的增量价值** = 逆转裁判特有机制（证言/询问/搜证/心锁/异议演出）+ 让非程序员也能用的**可视化编辑器**。

**分工边界：**

| 层 | 归属 | 职责 |
|----|------|------|
| 游戏引擎（渲染/存档/打包） | Ren'Py 原生 | 不改动 |
| 逆转裁判机制运行时 | 本项目 `renpy/common/00aa_*.rpy` | 已完成 ~80% |
| 数据格式（案件/流程/素材） | 本项目 JSON Schema | 待设计 |
| 可视化编辑器 | 本项目（桌面 App） | 本次转型的主角 |

---

## 三、核心架构：数据驱动的解释器（而非代码生成）

这是整个项目最重要的一次架构决策。

**原则：编辑器只产出数据（JSON），运行时读数据去解释执行。全程只有一份「事实来源」= `case.json`。**

```
可视化编辑器 (桌面App)
   │  拖拽/填表/导入素材
   ▼
case.json + evidence.json + characters.json + locations.json + 素材文件
   │  （唯一事实来源）
   ▼
Ren'Py 运行时
   ├── 流程解释器 00aa_runtime.rpy  ← 读 case.json 的节点数组，逐节点分派
   └── 逆转裁判虚拟机 00aa_*.rpy    ← 现有自定义语句下沉为运行时执行函数
   ▼
玩家可玩的游戏
```

**为什么不用「生成 .rpy 脚本」的方案：**

- 若编辑器生成 `.rpy`，则同时存在 JSON 和 .rpy 两份数据，极易失同步；玩家一旦手改 .rpy 就回不到编辑器。
- 数据驱动方案下，「改 JSON → 立即重载预览」天然成立，这是面向玩家的**实时预览杀手级体验**的前提。

**已有资产如何复用：** 现有 `00aa_statements.rpy` 里的 `execute_begin_testimony` / `execute_press` / `execute_present` 等执行函数**全部保留**，只是调用它们的角色从「手写的 .rpy 脚本」换成「流程解释器读 JSON 后分派」。虚拟机不用重写。

---

## 四、技术栈

### 运行时（游戏侧）
- **引擎**：Ren'Py（Python）
- **逆转裁判机制**：`renpy/common/00aa_*.rpy`（自定义 Statement / Screen / Displayable）
- **数据格式**：JSON + JSON Schema（可校验、可被编辑器读写）
- **画面基准**：1920×1080 (16:9)

### 编辑器（制作侧）
- **框架**：Tauri（Rust 壳 + Web 前端）—— 打包成桌面 App，玩家双击即用，可读写本地素材文件夹
- **UI**：React + TypeScript + Vite
- **流程画布**：节点连线式编辑器（推荐 React Flow）—— 因为逆转裁判的分支跳转本质是有向图
- **状态/校验**：以 `case.json` 的 JSON Schema 为准，编辑器读写并做合法性校验

### 分发
- 编辑器：桌面安装包（Tauri 产物）
- 玩家做出的游戏：调用 Ren'Py 打包，产出各平台可执行文件 / Web 版

---

## 五、数据格式设计原则

- **人类可读可写**：即使不用编辑器，手改 JSON 也能懂。
- **单一事实来源**：`case.json` 是流程的唯一真相，编辑器和运行时都以它为准。
- **版本兼容**：每个 JSON 带 `version` 字段，便于格式演进。
- **自包含可分享**：一个案件 = 一个文件夹（含所有 JSON + 素材），打包即可分享，社区互传。
- **引用而非内联**：证物/角色/地点集中定义，流程节点通过 ID 引用（编辑器里表现为下拉框选择，而非手敲 ID）。

---

## 六、开发路线

> 转型分为「打地基」和「盖房子」两大块。地基（数据驱动闭环）必须先于编辑器完成，否则编辑器无处安放。

### 阶段 0 — 现状盘点与收尾（Ren'Py 运行时）`进行中`
- 实际运行现有 `test_case.rpy`，验证 Step 8/9 遗留的 `renpy.execute()` 隐患
- 补齐「说到没做到」的自定义 TextTag（`aa_speed` / `aa_shake` / `aa_pause`）
- 整理文档，删除历史遗留的自研 React 引擎叙述

### 阶段 1 — 数据驱动地基（最关键）
- **设计 `case.json` 流程节点 Schema**（对话 / 搜证 / 证言询问 / 分支 / 演出，含分支跳转）
- **编写流程解释器 `00aa_runtime.rpy`**：读 `case.json` 逐节点分派到现有执行函数
- **把 `test_case.rpy` 改造为数据驱动**：流程从写死的 label/jump 变为读 JSON
- **验证闭环**：用手改 JSON 的方式（不做 UI）确认「改数据 → 表现变化」成立

### 阶段 2 — 编辑器 MVP
- Tauri + React + React Flow 脚手架
- 三大核心区：素材导入区 / 流程编排画布 / 属性面板
- 节点模板化（拖一个「证言询问」预制块，填空即可）
- 一键预览：启动 Ren'Py 运行时加载当前 `case.json`
- 案件工程的新建 / 打开 / 保存（读写本地文件夹）

### 阶段 3 — 内置素材库与易用性
- 内置逆转裁判系列经典人物立绘/动画默认素材库（成步堂、绫里真宵、御剑等）
- 内置标准音效集（异议/待った/くらえ/嘟嘟声/叮/拍桌/扣血/法官锤/BGM）
- 原创角色支持：本地导入图片，可视化标注表情/动画帧
- 校验与提示：缺失证物引用、断裂的流程连线、未设置正确举证等实时报错

### 阶段 4 — 高级演出与特殊玩法
- 角色动画状态机可视化编辑（在文本特定位置触发动画）
- 异议全屏冲击、屏幕震动、法官锤等演出的可视化配置
- 心灵枷锁、高潮推理、逻辑象棋等特殊环节的节点化
- 逆转检事式「搜证中插入询问」「证言分支」

### 阶段 5 — 分发与社区
- 一键导出：调用 Ren'Py 打包为各平台可执行文件 / Web 版
- 案件打包/分享格式，社区互传
- 模板案件、教程、文档
- 素材市场 / 社区素材分享机制（远期）

---

## 七、里程碑判定标准

| 里程碑 | 判定标准 |
|--------|----------|
| M1 地基通了 | 完全不碰 .rpy 代码，仅手改 `case.json` 即可改变游戏流程 |
| M2 编辑器能用 | 一个非程序员能在编辑器里做出「一段对话 + 一个搜证点 + 一次举证」并预览 |
| M3 素材无门槛 | 零素材玩家可用内置素材库做出完整一小节，也能导入原创角色 |
| M4 可分发 | 玩家能一键把作品导出为可执行文件发给朋友玩 |

---

## 八、最终的制作器到底支持哪些操作

> 本板块是项目的「北极星」——描述编辑器**做完之后**，一个玩家坐在电脑前能实际完成的所有操作。用于对齐愿景、拆解功能、验收进度。条目会随讨论持续增补。
>
> 标记说明：`[MVP]` 首个可用版本必备 · `[核心]` 逆转裁判核心体验 · `[进阶]` 提升品质 · `[远期]` 锦上添花

### 8.1 工程与项目管理

- `[MVP]` 新建一个案件工程（自动生成标准文件夹结构与空白 JSON）
- `[MVP]` 打开、保存、另存已有案件工程
- `[核心]` 一个案件包含多个章节，每章包含多个环节（搜证/法庭/特殊）
- `[进阶]` 案件设置：标题、作者、封面、简介、通关条件
- `[进阶]` 工程内搜索（找某句台词、某个证物在哪些节点被引用）
- `[远期]` 版本历史 / 撤销重做的工程级快照
- `[远期]` 多人协作 / 云端同步
- `[进阶]` 存档点配置：设置哪些节点允许玩家手动存档（如仅章节间可存，禁止在证言中途存档）
- `[进阶]` 游戏内笔记/备忘录：配置是否为本案件开启内置笔记功能及初始内容（对应 GS5/6 笔记本）

### 8.2 剧情文本编辑

- `[MVP]` 打字输入一段剧情对话并保存（选择说话角色 + 输入文本）
- `[MVP]` 旁白文本（无说话人）
- `[核心]` 一段文本中插入停顿、变速（在指定位置慢下来/快起来制造悬念）
- `[核心]` 文本内标记情绪效果：某几个字震动、放大、变色
- `[核心]` 为角色配置嘟嘟声音色（辩护方/检察方/证人/法官不同音色）
- `[进阶]` 文本中内嵌触发点：文字显示到某个位置时触发一个事件（换表情、播音效、屏幕震动、播放动画）
- `[进阶]` 富文本预览：编辑时就能看到最终排版效果
- `[远期]` 多语言文本管理（同一句话的多语言版本）

### 8.3 角色管理

- `[MVP]` 从内置素材库选择一个经典角色加入案件
- `[MVP]` 导入本地图片创建原创角色立绘
- `[核心]` 为角色设置多个动画状态：idle / talk / objection / breakdown / sweat / slam / think / shock / smile / angry / sad
- `[核心]` 可视化标注：把导入的图片指定为「说话帧/待机帧/异议帧」等
- `[核心]` 设置角色名字、名牌颜色、默认站位（左/中/右）
- `[进阶]` 表情部件系统：眼/嘴/眉分层，组合出多种表情
- `[进阶]` 逐帧动画编辑：为「崩溃」「拍桌」等设置多帧序列与时长
- `[进阶]` 角色档案（Profile）：可在游戏内法庭记录中查看的人物资料
- `[远期]` 从原作参考自动切图 / 精灵表(sprite sheet)自动拆帧

### 8.4 证物与法庭记录

- `[MVP]` 创建证物：名称、描述、图标（内置或本地导入）
- `[核心]` 证物检查大图（可多角度）
- `[核心]` 证物更新：获得新信息后描述/图标发生变化
- `[核心]` 证物组合：两件证物合成新证物
- `[进阶]` 证物标签分类（法庭记录 / 人物档案）
- `[进阶]` 设置证物的获得时机（搜证某热点后、举证成功后）
- `[进阶]` 证物特写检查：配置证物的可点击检查热点与发现时触发台词

### 8.5 搜证场景制作

- `[MVP]` 新建一个搜证场景，导入本地图片作为背景
- `[MVP]` 在背景图上直接框选/点选设置「搜证点（热点）」
- `[核心]` 为每个搜证点设置：名称、调查后显示的旁白文本、是否获得证物
- `[核心]` 设置玩家角色初始位置与移动方式（鼠标点击移动 / WASD）
- `[核心]` 放置 NPC：设置立绘、位置、可聊话题、出示证物的反应
- `[核心]` 地点导航：多个地点之间跳转，某地点在满足条件后才解锁
- `[进阶]` 搜证点/NPC/地点的条件出现（拥有某证物、完成某对话后才出现）
- `[进阶]` 热点交互提示气泡、放大镜光标等视觉配置
- `[进阶]` 搜证过程中插入询问环节（逆转检事式）
- `[核心]` 话题解锁链：NPC 的对话话题可依赖"先聊过某话题"或"已持有某证物"才出现
- `[核心]` 出示证物给 NPC：向 NPC 出示特定证物触发专属对话或解锁新地点，区别于普通调查反应

### 8.6 法庭 / 询问环节制作

- `[MVP]` 制作一段法庭对话
- `[核心]` 创建证言：逐条录入证人陈述，设置证言标题与证人
- `[核心]` 为某条陈述设置「威慑」反应：追问对话、是否追加新证言、是否扣血
- `[核心]` 为某条陈述设置「正确举证」：选择正确证物（下拉框），举证成功后的剧情、跳转
- `[核心]` 设置举证错误的惩罚（扣血格数）
- `[核心]` 配置血槽初始格数与 Game Over 触发
- `[进阶]` 双证物组合举证
- `[进阶]` 证言分支：威慑某条证言导致证言走向不同分支（证言树）
- `[进阶]` 证言回溯：玩家可随时重看已解锁证言
- `[核心]` 证言更新：威慑后可令证言原地插入新条目（对应原作"证言更新"节奏）
- `[进阶]` 察觉系统（Perceive）：配置证人的动作闪动帧，玩家盯视指定帧后触发特定矛盾揭穿
- `[进阶]` 证言条件锁定：某条陈述初始灰显，须先威慑/举证其他特定条目后才能操作

### 8.7 演出效果编排

- `[MVP]` 触发「异议！」全屏冲击（选择角色 + 样式：辩护/检察）
- `[核心]` 「待った！」「くらえ！」冲击演出
- `[核心]` 在剧情文本进行到特定位置时触发角色动画（例：说到某字时角色拍桌）
- `[核心]` 屏幕震动、画面闪白
- `[核心]` 播放/切换 BGM 与音效
- `[进阶]` 法官敲锤、证人席聚光灯、回忆闪回滤镜
- `[进阶]` 场景转场动画（搜证→法庭）、章节标题卡
- `[进阶]` 慢镜头、黑白闪等高级转场
- `[远期]` 自定义演出时间轴（多个演出并行/串行编排）

### 8.8 流程编排（节点画布）

- `[MVP]` 以节点方式排列环节：对话 → 搜证 → 证言 → 举证，用连线决定先后顺序
- `[核心]` 分支节点：根据玩家选择 / 举证正误 走向不同后续节点
- `[核心]` 跳转与循环（如举证失败回到证言展示）
- `[核心]` 选择菜单节点（给玩家几个对话选项）
- `[进阶]` 条件节点：根据是否拥有某证物 / 某 flag 决定走向
- `[进阶]` 变量与 flag 的可视化设置与判断
- `[进阶]` 画布缩放、分组、注释，便于管理大型案件
- `[远期]` 子流程 / 可复用流程模板
- `[核心]` 日期/时间节点：在画布上插入日期时间戳卡，玩家游戏中看到"第X天 下午 地方法院"式场景标题
- `[进阶]` 画布筛选/高亮：按日期、按询问、按演出类型等维度筛选，快速定位并高亮相关节点，辅助检查时间线逻辑

### 8.9 特殊玩法系统

- `[进阶]` 心灵枷锁：设置角色、锁环层数、每层正确证物、错误反应、全解开后的剧情
- `[进阶]` 高潮推理（Revisualization）：设置逻辑问题、每题正确答案、错误惩罚
- `[进阶]` 逻辑象棋（Mind Chess）：设置对弈轮次、攻防选项、倒计时、胜负分支
- `[进阶]` 逻辑碎片系统（逆转检事式）：调查中收集逻辑碎片，在逻辑整理界面拖拽连接两块碎片推出结论，配置每对碎片的连接条件与结论文本
- `[远期]` 看穿系统、情绪矩阵等更冷门机制

### 8.10 内置素材库

- `[MVP]` 内置逆转裁判系列经典人物立绘与动画（成步堂龙一、绫里真宵、御剑怜侍、糸锯圭介等）作为默认素材
- `[MVP]` 内置标准音效集：异议/待った/くらえ语音、嘟嘟声（多音色）、叮、拍桌、扣血、法官锤、Game Over、判决音效
- `[核心]` 内置标准 BGM（法庭/搜证/紧迫/推理氛围曲）
- `[核心]` 内置法庭/搜证背景图占位素材
- `[核心]` 内置 UI 素材（血槽、证物面板、按钮、进度点等）
- `[核心]` **原创素材导入**：经典素材不满足时，玩家从本地导入自己的图片/音频
- `[进阶]` 素材分类、搜索、预览、收藏
- `[进阶]` UI 主题切换（GBA 复古风 / HD 现代风 / 大逆转裁判风）
- `[远期]` 社区素材市场：下载他人分享的角色/背景/音效包

### 8.11 预览与调试

- `[MVP]` 一键预览：直接跑当前案件（或从选中节点开始跑）
- `[核心]` 从任意节点开始预览（无需每次从头玩）
- `[进阶]` 实时预览：改动后快速重载，无需重启
- `[进阶]` 校验报告：列出所有问题（缺失素材、断裂连线、未设正确举证的证言、引用了不存在的证物等）
- `[远期]` 单步调试 / 查看运行时变量状态

### 8.12 导出与分享

- `[MVP]` 导出案件工程包（含所有 JSON + 素材），可发给他人用编辑器打开
- `[核心]` 一键导出为可玩游戏：调用 Ren'Py 打包为 Windows/Mac/Linux 可执行文件
- `[进阶]` 导出为 Web 版（浏览器可玩）
- `[进阶]` 导出为 Android APK
- `[远期]` 发布到社区平台 / 生成分享链接

---

## 九、给会写代码玩家的「逃生舱口」

编辑器面向非程序员，但不锁死高级用户：

- 导出的案件工程是**开放的 JSON + Ren'Py 项目**，可以直接用文本编辑器 / Ren'Py SDK 打开继续深度改造。
- 数据格式公开且有 Schema，欢迎社区在其上写自己的工具。
- 但**编辑器本身不承诺兼容手改后的产物再导回**——一旦离开编辑器做代码级改造，就是高级用户自己的领域了。这是有意为之的边界，避免编辑器为了兼容一切而变得臃肿。
